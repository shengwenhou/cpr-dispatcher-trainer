# 技術債登記簿

> 已知的取捨與待還債項。新增債務時記錄「為何當時這樣選」，償還後移到頁尾「已償還」區。

## 未償還

| # | 項目 | 成因／當時的取捨 | 風險 | 償還時點 |
|---|---|---|---|---|
| 1 | **STT spike 未做**：SpeechTranscriber(zh_TW) 對壓胸數數的行為未驗證 | 規劃階段先定架構、風險驗證排第一個開發任務 | 高——若不通過需切 whisper.cpp（介面已預留，影響可控） | Phase 0（開發第一件事） |
| 2 | **Swift helper 未寫**：唯一非 Python 元件，依賴 macOS 26 API | on-device STT 的必要橋接，換取零費用零網路 | 中——macOS 大版本更新可能動 API；寫完後極少改 | Phase 0 雛形、Phase 2 定稿 |
| 3 | **意圖分類依賴雲端 API** | 起步省事、每場 <NT$1；地端化延後 | 低——斷網課堂不可用（v1.x 期間需網路） | v2.0（ollama + Qwen3-4B） |
| 4 | **「開始按壓」時間戳為 self-report**（靠學員數數的語音佐證） | 純語音架構下最可靠訊號；教學用途足夠 | 低——研究級精度不足 | v1.2（音訊節奏偵測或假人感測） |
| 5 | **教材 MD 轉檔噪音**：bullet 符號殘留（ϭ）、QI 表單的「City」佔位欄 | pymupdf4llm 快速轉檔的品質取捨；不影響閱讀與引用 | 低 | 引用到相關段落時順手清 |
| 6 | **教材存放分立**：公開 toolkit 二份正本在 repo（私有文件區另有副本）；In-House protocol／QI 表單正本只在維護者私有文件區（版權考量不入 repo） | 公開 repo 的版權清潔 vs 開發便利的取捨 | 低——開發時需本地讀取私有區檔案 | 不需償還（設計如此）；台詞定稿後清理私有區的公開 toolkit 舊副本 |
| 7 | **Wiki 與 SPEC 同步靠紀律**（人為債） | wiki 摘要 + SPEC 正本的分工，換取單一權威來源 | 低——規格變更時 wiki 可能過時 | 每次改 SPEC 的 commit 順手檢查 wiki |
| 8 | **GitHub 認證綁單機 keyring** | 單一工作機模式下最簡 | 極低——筆電獨立 clone 需另行 `gh auth login`（記錄性質，非缺陷） | 不需償還 |
| 9 | **Wiki 暫居主 repo `wiki/` 目錄** | GitHub Free 方案的 private repo 不支援 Wiki 功能（需 Pro 或轉 public）；內容已按 Wiki 格式備妥，本地 `~/Projects/cpr-dispatcher-trainer.wiki/` 待命 | 低——`[[連結]]` 語法與側邊欄在目錄瀏覽下不生效 | 使用者升級 GitHub Pro 或將 repo 轉 public 後，一個 push 完成遷移 |

## 設計上「刻意不做」（非債務，避免誤還）

- 地址複誦（動態 TTS）——刻意繞開以保 100% 預錄，已定案
- 救護車抵達計時——課堂節奏權在講師，已定案
- 純 LLM 自由對話——與訓練一致性衝突，架構層面已否決

## 已償還

| 項目 | 償還記錄 |
|---|---|
| codex CLI 安裝損壞（npm 原生執行檔遺失） | 2026-07-03 重裝 0.142.5 並更新環境快照 |
